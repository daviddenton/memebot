.create-modal
    .create-meme
        p.title
        img.create-meme
        form.create-meme(action='/', method='POST')
            input.control(id='topText', placeholder='top caption')
            input.control(id='bottomText', placeholder='bottom caption')
            input.control(id='generatorId', type='hidden')
            input.control(id='imageId', type='hidden')
            button.control(type='submit')
                p go!
script(type='text/javascript', src='/js/createMeme.js')
